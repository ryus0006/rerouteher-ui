# `npm run dev` talks to the real backend below. Override VITE_API_BASE_URL in
# .env.local if your local backend is on a different port (local-start picks 8080,
# or 8081 if 8080 is busy).
VITE_API_BASE_URL=http://localhost:8081
